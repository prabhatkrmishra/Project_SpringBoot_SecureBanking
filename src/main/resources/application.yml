# banking.pkmprojects.online

server:
  port: 8091

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/banking_schema
    username: banking
    password: banking@pkmprojects
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

jwt:
  secret: banksecurityapplicationjwtsecretkeybanksecurityapplication